# Puerto en el que corre el servidor local del bar
PORT=3000

# Clave de API de Google Gemini (Opcional, para el generador de playlists con IA)
# Si no se define, el sistema usa el clasificador heurístico offline incorporado
GEMINI_API_KEY=
